using Core;

namespace Core
{
    using System;
    
    public partial class Materialxportableportal
    {
        public static void Fourfourthquatenaryquadruple()
        {
            IsNegatehole = Object.Equals(BlackholeCharacterArray[Error], WhiteholeCharacterArray[Error]) is true;

            if (IsNegatehole)
            {
                var symmetry = 0;

                symmetry = symmetry + (Int32)BlackholeCharacterArray[Error];

                symmetry = symmetry + (Int32)WhiteholeCharacterArray[Error];

                symmetry = (Int32)((Single)symmetry / 2.00F);

                var convert = Convert.ToChar(symmetry);

                NegateholeCharacterArray[Error] = convert;
            }
            else
            {
                NegateholeCharacterArray[Error] = (Char)Materialxportableascii.EntityWhitespace;
            }

            return;
        }
    }
}
