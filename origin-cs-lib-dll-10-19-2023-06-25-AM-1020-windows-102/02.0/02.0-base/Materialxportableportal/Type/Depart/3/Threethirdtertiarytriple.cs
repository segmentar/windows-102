using Core;

namespace Core
{
    using System;

    public partial class Materialxportableportal
    {
        public static void Threethirdtertiarytriple()
        {
            IsAdditivehole = Object.Equals(BlackholeCharacterArray[Error], WhiteholeCharacterArray[Error]) is false;

            if (IsAdditivehole)
            {
                var left = (((Int32)BlackholeCharacterArray[Error] == Materialxportableascii.EntityWhitespace) ? Materialxportableascii.EntityNull : BlackholeCharacterArray[Error]);
                  
                var right = (((Int32)WhiteholeCharacterArray[Error] == Materialxportableascii.EntityWhitespace) ? Materialxportableascii.EntityNull : WhiteholeCharacterArray[Error]);             

                var asymmetry = Math.Max(left, right);

                var convert = Convert.ToChar(asymmetry);

                AdditiveholeCharacterArray[Error] = convert;
            }
            else
            {
                AdditiveholeCharacterArray[Error] = (Char)Materialxportableascii.EntityWhitespace;
            }

            return;
        }
    }
}
