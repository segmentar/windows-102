using Core;

namespace Core
{
    using System;

    using System.Drawing;

    public partial class Materialxportableupscale
    {
        public static Bitmap Upscale(Object reflect_OBJECT, Single Factor_VALUE)
        {
            Bitmap bitmapResult = default;

            bitmapResult = ZeroZeroth(reflect_OBJECT, Factor_VALUE);

            return bitmapResult;
        }
    }
}
