using Core;

namespace Core
{
    using System;

    using System.IO;

    public partial class ExpressionxportablemoduleSeal
    {
        public void MAN()
        {
            var array = Expressionxportableio.ExpressionxportableFileSetSurface(DirectoryInfo.FullName, true);

            var list = Expressionxportablemagic.ExpressionxportablemagicArrayListCastDispenser(Desorb[2]);

            foreach (FileInfo fileInfo in array)
            {
                var name = Path.GetDirectoryName(fileInfo.FullName);

                var lower = name.ToLower();

                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = Object.Equals(Expressionxportablestorehexagon.EntityNAL, lower) is true;

                shouldContinueCheck = isEqualCheck is false;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                list.Add(fileInfo);
                
                continue;
            }

            foreach (FileInfo fileInfo in array)
            {
                var name = Path.GetDirectoryName(fileInfo.FullName);

                var lower = name.ToLower();

                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = Object.Equals(Expressionxportablestorehexagon.EntityHAM, lower) is true;

                shouldContinueCheck = isEqualCheck is false;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                list.Add(fileInfo);

                continue;
            }

            foreach (FileInfo fileInfo in array)
            {
                var name = Path.GetDirectoryName(fileInfo.FullName);

                var lower = name.ToLower();

                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = Object.Equals(Expressionxportablestorehexagon.EntityMAN, lower) is true;

                shouldContinueCheck = isEqualCheck is false;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                list.Add(fileInfo);

                continue;
            }

            foreach (FileInfo fileInfo in array)
            {
                var name = Path.GetDirectoryName(fileInfo.FullName);

                var lower = name.ToLower();

                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = Object.Equals(Expressionxportablestorehexagon.EntityTAK, lower) is true;

                shouldContinueCheck = isEqualCheck is false;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                list.Add(fileInfo);

                continue;
            }

            foreach (FileInfo fileInfo in array)
            {
                var name = Path.GetDirectoryName(fileInfo.FullName);

                var lower = name.ToLower();

                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = Object.Equals(Expressionxportablestorehexagon.EntitySAJ, lower) is true;

                shouldContinueCheck = isEqualCheck is false;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                list.Add(fileInfo);

                continue;
            }

            foreach (FileInfo fileInfo in array)
            {
                var name = Path.GetDirectoryName(fileInfo.FullName);

                var lower = name.ToLower();

                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = Object.Equals(Expressionxportablestorehexagon.EntityKAI, lower) is true;

                shouldContinueCheck = isEqualCheck is false;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                list.Add(fileInfo);

                continue;
            }

            foreach (FileInfo fileInfo in array)
            {
                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = list.Contains(fileInfo) is true;

                shouldContinueCheck = isEqualCheck is true;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                list.Add(fileInfo);

                continue;
            }

            FileInfoArray = new FileInfo[list.Count];

            list.CopyTo(FileInfoArray, ExpressionxportablePolicy.ExpressionxportableIndexPolicy);

            Fourfourthquaternaryquadruple();

            list.Clear();

            return;
        }
    }
}
