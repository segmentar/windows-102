using Core;

namespace Core
{
    using System;

    using System.IO;

    public partial class ExpressionxportablemoduleSeal
    {
        public void NAL()
        {
            var value = Expressionxportable.ExpressionxportableRootVariation();

            var text = File.ReadAllText(FileInfo.FullName);

            Expressionxportableschedule.GroupScheduleText(value, text);

            var result = Expressionxportablereload.GroupReloadInExpressionFull(value, Path.GetRandomFileName());

            var list = Expressionxportablemagic.ExpressionxportablemagicArrayListCastDispenser(Expressionxportable.ActiveArrayListObject);

            list.Add(result);

            Fivefifthquinaryquintuple();

            return;
        }
    }
}
