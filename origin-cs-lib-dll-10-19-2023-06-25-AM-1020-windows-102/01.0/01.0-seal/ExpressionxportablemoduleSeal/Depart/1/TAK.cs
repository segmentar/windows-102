using Core;

namespace Core
{
    using System;

    public partial class ExpressionxportablemoduleSeal
    {
        public void TAK()
        {
            var list = Expressionxportablemagic.ExpressionxportablemagicArrayListCastDispenser(Desorb[0]);

            foreach (String stringValue in Expressionxportablemagic.ExpressionxportablemagicArrayListCastDispenser(ExpressionModule.ModuleArrayListObject))
            {
                list.Add(stringValue);

                continue;
            }

            ModuleArray = new String[list.Count];

            list.CopyTo(ModuleArray, ExpressionxportablePolicy.ExpressionxportableIndexPolicy);

            Twosecondsecondarydouble();

            list.Clear();

            return;
        }
    }
}
