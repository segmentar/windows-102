using Core;

namespace Core
{
    using System;

    using System.IO;

    public partial class ExpressionxportablemoduleSeal
    {
        public void Threethirdtertiarytriple()
        {
            foreach (DirectoryInfo directoryInfo in DirectoryInfoArray)
            {
                DirectoryInfo = directoryInfo;

                MAN();

                continue;
            }

            return;
        }
    }
}
