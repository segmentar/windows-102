using Core;

namespace Core
{
    using System;

    public partial class ExpressionxportableexecuteSeal
    {
        public void Onefirstprimarysingle()
        {
            foreach (Expressionxportable expressionxportable in (Expressionxportable[])Deflect[0])
            {
                ExpressionExecute = expressionxportable;

                Boolean isEqualCheck, shouldContinueCheck;

                isEqualCheck = (expressionxportable.ObjectIdentity is Expressionxportableexecutearray).Equals(true);

                shouldContinueCheck = isEqualCheck is false;

                if (shouldContinueCheck is true)
                {
                    continue;
                }
                else
                    "false".ToString();

                TAK();

                continue;
            }

            return;
        }
    }
}
