using Core;

namespace Core
{
    using System;

    public partial class ExpressionxportableexecuteSeal
    {
        public void Threethirdtertiarytriple()
        {
            var index = 0;

            do
            {
                Boolean isEqualCheck, shouldBreakCheck;

                isEqualCheck = Object.Equals(ExecuteArray.Length, index) is true;

                shouldBreakCheck = isEqualCheck is true;

                if (shouldBreakCheck is true)
                {
                    break;
                }
                else
                    "false".ToString();

                Instruction = ExecuteArray[index];

                MAN();

                index = index + 1;

                continue;

            } while (true);

            return;
        }
    }
}
