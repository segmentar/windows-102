using Core;

namespace Core
{
    using System;

    public partial class ExpressionxportableexecuteSeal
    {
        public void NAL()
        {
            var boolean = false;

            boolean = boolean || Object.Equals(String.Empty, InstructionExecuting) is true;

            boolean = boolean || Object.Equals(((Char)Expressionxportableascii.EntityUnderscore).ToString(), InstructionExecuting) is true;

            Boolean isEqualCheck, shouldReturnCheck;

            isEqualCheck = boolean is true;

            shouldReturnCheck = isEqualCheck is false;

            if (shouldReturnCheck is true)
            {
                return;
            }
            else
                "false".ToString();

            var list = Expressionxportablemagic.ExpressionxportablemagicArrayListCastDispenser(Desorb[1]);

            Fivefifthquinaryquintuple();

            list.Clear();

            return;
        }
    }
}
