using Core;

namespace Core
{
    using System;

    public partial class ScopexportablesealExtract
    {
        public void Sixsixthsenarysextuple()
        {
            var list = Scopexportablemagic.ScopexportablemagicArrayListCastDispenser(Desorb[1]);

            if (ForgeAdd() is true)
            {
                list.Add(ScopexportableformValue);
            }
            else
                "false".ToString();

            KAI();

            return;
        }
    }
}
