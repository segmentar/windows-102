using Core;

namespace Core
{
    using System;

    public partial class ScopexportablesealExtract
    {
        public void Fivefifthquinaryquintuple()
        {
            var valid = true;

            valid = valid && StartHas is true;

            valid = valid && EndHas is true;

            valid = valid && DifferenceHas is true;

            valid = valid && StepHas is true;

            if (valid is true)
            {
                ValidIs = true;
            }
            else
                "false".ToString();

            SAJ();

            return;
        }
    }
}
