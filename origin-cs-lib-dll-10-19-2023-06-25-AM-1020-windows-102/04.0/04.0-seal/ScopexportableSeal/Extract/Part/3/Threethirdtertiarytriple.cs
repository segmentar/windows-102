using Core;

namespace Core
{
    using System;

    public partial class ScopexportablesealExtract
    {
        public void Threethirdtertiarytriple()
        {
            var start = true;

            var end = true;

            start = start && Object.Equals(Start, -1) is false;

            end = end && Object.Equals(End, -1) is false;

            if (start is true)
            {
                StartHas = true;
            }
            else
                "false".ToString();

            if (end is true)
            {
                EndHas = true;
            }
            else
                "false".ToString();

            MAN();

            return;
        }
    }
}
