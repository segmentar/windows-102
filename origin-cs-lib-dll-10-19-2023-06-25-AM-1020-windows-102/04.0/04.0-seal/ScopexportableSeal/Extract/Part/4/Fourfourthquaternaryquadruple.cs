using Core;

namespace Core
{
    using System;

    public partial class ScopexportablesealExtract
    {
        public void Fourfourthquaternaryquadruple()
        {
            var difference = true;

            difference = difference && (Difference < 0).Equals(false);

            difference = difference && (Difference < 1).Equals(false);

            if (difference is true)
            {
                DifferenceHas = true;
            }
            else
                "false".ToString();

            NAL();

            return;
        }
    }
}
