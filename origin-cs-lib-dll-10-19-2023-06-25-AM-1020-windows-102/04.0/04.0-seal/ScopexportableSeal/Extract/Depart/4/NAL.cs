using Core;

namespace Core
{
    using System;

    public partial class ScopexportablesealExtract
    {
        public void NAL()
        {
            var step = true;

            step = step && Object.Equals(Index, Start) is true;

            if (step is true)
            {
                StepHas = true;
            }
            else
                "false".ToString();

            Fivefifthquinaryquintuple();

            return;
        }
    }
}
