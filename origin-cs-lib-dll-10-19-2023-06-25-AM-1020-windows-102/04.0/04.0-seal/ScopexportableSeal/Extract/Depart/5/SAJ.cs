using Core;

namespace Core
{
    using System;

    public partial class ScopexportablesealExtract
    {
        public void SAJ()
        {
            if (ValidIs is true)
            {
                Scopexportableformcoordinate scopexportableform;

                scopexportableform = new Scopexportableformcoordinate();

                scopexportableform.FileInfo = ScopexportablescansolidValue.FileInfo;

                scopexportableform.DirectoryInfo = ScopexportablescansolidValue.DirectoryInfo;

                scopexportableform.Text = ScopexportablescansolidValue.Text;

                scopexportableform.Start = Start;

                scopexportableform.End = End;

                ScopexportableformValue = scopexportableform;
            }
            else
                "false".ToString();

            Sixsixthsenarysextuple();

            return;
        }
    }
}
