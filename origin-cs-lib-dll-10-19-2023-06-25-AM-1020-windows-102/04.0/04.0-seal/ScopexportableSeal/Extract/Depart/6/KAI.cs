using Core;

namespace Core
{
    using System;

    public partial class ScopexportablesealExtract
    {
        public void KAI()
        {
            var list = Scopexportablemagic.ScopexportablemagicArrayListCastDispenser(Desorb[1]);

            var array = new Scopexportableformcoordinate[list.Count];

            list.CopyTo(array, ScopexportablePolicy.ScopexportableIndexPolicy);

            ScopexportableformArray = array;

            StartHas = false;

            EndHas = false;

            DifferenceHas = false;

            StepHas = false;

            ValidIs = false;

            return;
        }
    }
}
