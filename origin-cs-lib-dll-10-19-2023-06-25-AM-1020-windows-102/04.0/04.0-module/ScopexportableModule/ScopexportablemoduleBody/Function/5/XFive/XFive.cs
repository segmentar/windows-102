using Core;

namespace Core
{
    using System;

    public partial class ScopexportablemoduleBody
    {
        public void XFive()
        {
            var value = XQuintuple.ForgeLevel(ScopexportablemoduleBodycycle.XQuaternary.XQuadruple.IjklmnArray, (Scopexportableformheadersolid[])ScopexportablemoduleBodycycle.XZeroth.ObjectArray[2]);

            XFifth xfifth;

            xfifth = new XFifth(value);

            ScopexportablemoduleBodycycle.XQuinary = xfifth;

            return;
        }
    }
}
