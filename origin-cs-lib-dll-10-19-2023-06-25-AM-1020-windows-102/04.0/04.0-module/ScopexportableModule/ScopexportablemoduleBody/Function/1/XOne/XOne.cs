using Core;

namespace Core
{
    using System;

    public partial class ScopexportablemoduleBody
    {
        public void XOne()
        {
            var value = XSingle.ForgeIjklmn((Scopexportableformcoordinate[])ScopexportablemoduleBodycycle.XZeroth.ObjectArray[0], (Object[])ScopexportablemoduleBodycycle.XZeroth.ObjectArray[1]);

            XFirst xFirst;

            xFirst = new XFirst(value);

            ScopexportablemoduleBodycycle.XPrimary = xFirst;

            return;
        }
    }
}
