using Core;

namespace Core
{
    using System;

    public partial class ScopexportablemoduleHeader
    {
        public partial class XSingle
        {
            public static XSingle ForgeIjklmn(Scopexportableformcoordinate[] array_SCOPEXPORTABLEFORMCOORDINATE, Object[] array_OBJECT)
            {
                XSingle xsingleResult = default;

                var array = FunctionIjklmnSetSurface(array_SCOPEXPORTABLEFORMCOORDINATE, array_OBJECT);

                XSingle xsingle;

                xsingle = new XSingle(array);

                xsingleResult = xsingle;

                return xsingleResult;
            }
        }
    }
}
