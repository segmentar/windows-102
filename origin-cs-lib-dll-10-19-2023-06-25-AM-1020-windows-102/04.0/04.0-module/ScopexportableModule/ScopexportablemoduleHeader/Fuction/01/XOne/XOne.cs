using Core;

namespace Core
{
    using System;

    public partial class ScopexportablemoduleHeader
    {
        public void XOne()
        {
            var value = XSingle.ForgeIjklmn((Scopexportableformcoordinate[])ScopexportablemoduleHeadercycle.XZeroth.ObjectArray[0], (Object[])ScopexportablemoduleHeadercycle.XZeroth.ObjectArray[1]);

            XFirst xfirst;

            xfirst = new XFirst(value);

            ScopexportablemoduleHeadercycle.XPrimary = xfirst;

            return;
        }
    }
}
