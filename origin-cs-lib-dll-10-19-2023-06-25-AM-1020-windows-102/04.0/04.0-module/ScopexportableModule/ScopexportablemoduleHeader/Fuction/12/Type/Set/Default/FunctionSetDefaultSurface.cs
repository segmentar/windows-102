using Core;

namespace Core
{
    using System;

    public partial class ScopexportablemoduleHeader
    {
        public partial class XDuodecuple
        {
            public static Scopexportableformheadersolid[] FunctionDefaultSetSurface(ScopexportableijklmnHeaderUopqr_tV[] Ijklmn_ARRAY)
            {
                Scopexportableformheadersolid[] arrayResult = default;

                var list = FunctionDefaultSet(Ijklmn_ARRAY);

                var array = new Scopexportableformheadersolid[list.Count];

                list.CopyTo(array, ScopexportablePolicy.ScopexportableIndexPolicy);

                arrayResult = array;

                return arrayResult;
            }
        }
    }
}
