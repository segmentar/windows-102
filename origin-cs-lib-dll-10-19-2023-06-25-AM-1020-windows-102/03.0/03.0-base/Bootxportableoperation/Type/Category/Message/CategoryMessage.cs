using Core;

namespace Core
{
    using System;

    public partial class Bootxportableopcode
    {
        public static void CategoryMessage(Expressionxportable value_EXPRESSIONXPORTABLE, String[] array_STRING)
        {
            var join = String.Join(((Char)Bootxportableascii.EntityWhitespace).ToString(), array_STRING);

            Scopexportablemessageout.Message(join);

            return;
        }
    }
}
